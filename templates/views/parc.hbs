<div class="container">
	<div class="row">
		<div class="col-sm-10 col-sm-offset-1 col-md-8 col-md-offset-2">
			<article>
				<p><a href="/parcblog">&larr; back to the blog</a></p>
				<hr>
				<header>
					<h1>{{data.parc.title}}</h1>
					<h5>
						{{{categoryParcList data.parc.categories prefix="Posted in "}}}
						{{#if author.name.first}}by {{author.name.first}}{{/if}}
					</h5>
				</header>
				<div class="post">
					{{#if data.parc.image}}<img src="{{{cloudinaryUrl data.parc.image width=750 height=450 crop='fit' }}}" class="img-responsive">{{/if}}
					{{{data.parc.content.extended}}}
				</div>
			</article>
		</div>
	</div>
</div>
